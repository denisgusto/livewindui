{
    "name": "livewindui",
    "description": "Runtime JS bundle for LiveWindUI (served via @livewindScripts).",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite build --watch",
        "lint": "eslint js",
        "format": "prettier --write js",
        "test": "vitest run"
    },
    "devDependencies": {
        "eslint": "^9.0.0",
        "prettier": "^3.0.0",
        "vite": "^6.0.0",
        "vitest": "^2.1.0"
    }
}
